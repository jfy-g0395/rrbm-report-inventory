# DELIVERY-SCHEDULE-FEATURE

Implementation tracker for two features under a new **Delivery Schedule** tab:
**A) Scheduled inter-warehouse stock moves (request → admin approve → complete)** and
**B) Deferred order delivery (order records nothing until delivered).**

## How to use this doc
- Work **one session at a time** (each is scoped to stay usage-efficient).
- At the **start** of a session: read "Locked decisions", "Reuse map", and the target session block.
- At the **end** of a session: tick its checkboxes, fill in the "State after this session" note, and confirm the "Next session" pointer. Do NOT start the next session in the same run.
- Every DB/verify step uses the **ephemeral-clone** workflow (clone prod via `pg_dump` into an isolated compose project; never test on live). Deploy = `docker compose up -d --build` (Flyway auto-runs) + static frontend cache-bust.

---

## Locked decisions (from planning Q&A)
1. Deferred delivery is chosen **at order creation**; the dead `order_type=DELIVERY` option is **removed** and replaced by this flow.
2. Stock-move quantities change **only on "Complete" (arrival)**, not on approval.
3. Approvers = **SUPER_ADMIN, ADMINISTRATOR, and a new assignable `DELIVERY_MANAGEMENT` role**.
4. A stock-move request holds **multiple products** (per-line from→to warehouse).
5. **Cancellation/lifecycle (no gaps):** a `SCHEDULED_DELIVERY` order records nothing until it resolves to exactly one terminal state — `DELIVERED` or `CANCELLED`. Actions: **Mark Delivered** (record on the day), **Reschedule** (move date, repeatable indefinitely), **Deliver/record now** (un-schedule but keep + record today), **Cancel order** (drop, nothing recorded). No path leaves an order alive-but-unrecorded. A passed date never auto-acts (flagged "overdue").
6. **WH3 = "Balagtas" everywhere** — remove the "Santan" label from code (but NOT the real street address `116 Santan St.`, nor immutable migration history).

## Reuse map (grounding — file:line)
- Warehouses: `products.stock_wh1/2/3`; `Product.getTotalStock()`. WH3 label = "Balagtas".
- `InventoryService`: `addWhStock`, `deductWhStock`, `requireValidWarehouse`, `logMovement`; `inventory_movements` already allows `movement_type='TRANSFER'` (no movement-type migration needed).
- `OrderService.createOrder` (L180) deducts stock + `TransactionService.recordSale` + commission at creation. `createOrderAtDate` has an `affectStock` flag + effective date. `batchMarkAsCollected` (L1179) = the template for "fulfill later, record on the real day".
- Daily-report status filter: `status NOT IN ('CANCELLED','PENDING','PENDING_COLLECTION')` in `DailyReportService.populateSnapshot`/`closeDailySales`.
- `orders.chk_status` CHECK constraint (widen like V88 did for `chk_source`).
- Roles: `UserController.VALID_ROLES` + `ROLE_DEFAULT_PAGES` + `ALL_PAGES` (L24–43) → `allowedPages`; frontend `PAGE_PERMISSIONS` (app.js:188), role badge (app.js:212), `viewToPageKey` (app.js:379), `canAccessPage` (app.js:411).
- Frontend: `navigateTo` (app.js:5505); list load/render (`loadDailyReports`/`renderDailyReportsList` ~10295); modal `openModal/closeModal` (app.js:325); **product type-ahead `renderProductDropdown` (app.js:5964)** already shows `WH1:x · WH2:y · Balagtas:z` from `appState.cachedProducts` — reuse for the move modal; security-key gate `POST /api/auth/verify-security-key` (app.js:2925); order form type select (index.html:592); employee role selects (index.html ~3741/3902/4049); "Santan" label to fix: `ProductController.java:361/365`.
- Next Flyway version: **V95** (V90 = Reject-Management `chk_role` widening; V91 = roles→page-access task; **V92 was claimed by an unrelated branch — `feat/daily-report-pizza-boxes-dispatched` — merged + deployed 2026-07-04**, so Session 1's stock-transfers migration was renumbered **V92 → V94** to avoid a clash; V93 = Session 3's `order_scheduled_delivery` migration, already drafted; V94 = Session 1's `stock_transfers` migration, already drafted).

## ⚠️ Where the Session 1–4 code actually lives (2026-07-04)
**All four of Sessions 1, 2, 3, and 4 are drafted in full** (backend AND frontend, all files written) but the checkboxes still read `[ ]`/partial because none of it has been verified on an ephemeral clone yet. The code is **not lost** — it's parked in a git stash on this machine so it wouldn't get swept into the pizza-boxes-dispatched deploy:
- `git stash list` → `stash@{0}`: "delivery-schedule WIP: order-scheduled-delivery + stock-transfers (holding out of pizza-boxes deploy)"
- **Backend (Sessions 1 & 3):** `StockTransfer.java`, `StockTransferController.java`, `StockTransferItem.java`, `StockTransferRepository.java`, `StockTransferService.java`, `V94__stock_transfers.sql`, `Order.java`/`OrderController.java`/`OrderService.java`/`OrderResponse.java`/`CreateOrderRequest.java` changes, `V93__order_scheduled_delivery.sql`, `DashboardController.java`/`InventoryService.java`/`PageAccessInterceptor.java`/`ReportsController.java` changes.
- **Frontend (Sessions 2 & 4):** `index.html` (+136 lines) — "New Stock Move" button + status filter + `modal-stock-move` (multi-line request) + `modal-stock-move-action` (approve/complete/reject/reschedule/cancel); order-form "Schedule for later delivery" checkbox + date picker (old `DELIVERY` option removed) + `modal-delivery-action` (deliver/reschedule/cancel); cache-bust bumped to `?v=u14` in the stash (⚠️ **reconcile before redeploying** — live is currently `u12`, so this needs to become `u15`, not `u14`, since `u13` never shipped). `app.js` (+620 lines) — full JS for both: `loadStockMoves`, `openStockMoveModal`, `addStockMoveLine`, `submitStockMove`, `askApproveMove`/`askCompleteMove`/`askRejectMove`/`askRescheduleMove`/`askCancelMove`, `confirmStockMoveAction`, product autocomplete for move lines; `toggleScheduleDelivery`, `loadOrderDeliveries`, `askFulfillDelivery`/`askRescheduleDelivery`/`askCancelDelivery`, `confirmDeliveryAction`, oversell-warning helpers.
- Also in the stash: this doc's own in-progress edits.
- **Before resuming Session 1/2/3/4:** `git stash pop` (or `git stash apply stash@{0}` to keep the stash as a backup) to restore this code to the working tree. Don't delete the stash or treat the missing files as "cleaned up" — they're intentionally parked, not abandoned.

---

## ⏸ PAUSED — 2026-07-04 (Sat) · resume ~Mon 2026-07-06
**Where we are:** Sessions 0–4 code-complete and **restored to the `main` working tree** (stash applied 2026-07-04; stash kept as backup). Both features sit **uncommitted**; **nothing is deployed** — live prod Flyway is still at **V91** (V93 Phase B + V94 Phase A apply together on the next `docker compose up -d --build`; note V92 is the already-deployed pizza-boxes migration). Frontend cache-bust reconciled to `app.js?v=u15` (was `u14` in the stash; `u13` never shipped, live is `u12`).
**Resume here → Session 5** (the only remaining work): run the combined ephemeral-clone E2E for **both** features (stock-move request→approve→complete + deferred create→reschedule→fulfil→cancel/deliver-now, cancellation matrix, report-exclusion, role gating, and a normal-order regression), then **deploy** (`docker compose up -d --build`, confirm Flyway at **V94**), and **commit + push** (backend + frontend). Back up prod before deploying (per repo rules). No new code expected — Session 5 is verify + ship.
**Open question for the user (deferred):** whether to soften the live `OVERDUE` pill to sentence-case "Overdue" (cosmetic only).

## STATUS OVERVIEW
- [x] **Session 0** — Foundations: `DELIVERY_MANAGEMENT` role + Balagtas rename + empty Delivery Schedule tab (code-only, no migration)
- [x] **Session 1** — Stock Move backend (migration **V94** + entities/service/controller) — _verified via full REST E2E on clone (Session 5); deployed_
- [x] **Session 2** — Stock Move frontend (request modal + approval actions) → **Phase A shipped** — _deployed 2026-07-04, `?v=u15`_
- [x] **Session 3** — Deferred Delivery backend (migration **V93** + defer/fulfill/reschedule/cancel + report exclusion) — _verified via full REST E2E on clone (Session 5); deployed_
- [x] **Session 4** — Deferred Delivery frontend (order-form option + tab management) → **Phase B shipped** — _deployed 2026-07-04, `?v=u15`_
- [x] **Session 5** — Full E2E verification + deploy + commit — **DONE (2026-07-04): 48/48 clone E2E, deployed to prod at V94, pushed**
- [x] **Session 6** — Edit & Confirm scheduled deliveries (V95): edit line items before delivery + final-order confirmation gate — **DONE (2026-07-06): clone E2E green, deployed to prod at V95, pushed**

---

## Session 0 — Foundations (small, code-only, no DB migration)
**Goal:** add the new role, finish the Balagtas rename, and stand up the empty tab so later sessions have a home.
- [x] Backend `UserController`: added `"DELIVERY_MANAGEMENT"` to `VALID_ROLES` (L25); added its `ROLE_DEFAULT_PAGES` entry (`dashboard, orders, delivery-schedule, inventory, delivery-reports`); added `"delivery-schedule"` to `ALL_PAGES`.
- [x] Frontend `ROLE_DEFAULT_PAGES` (app.js): added `DELIVERY_MANAGEMENT` entry + `delivery-schedule` to `ADMINISTRATOR`/`ADMIN`. Added role badge label + `viewToPageKey('delivery-schedule')`.
- [x] Added `DELIVERY_MANAGEMENT` option to all **3 role `<select>`s** (add-emp / edit-emp / assign-role). `onRoleSelectChange` needed no change (reads `ROLE_DEFAULT_PAGES`); also added a `delivery-schedule` checkbox to both add/edit page-access grids.
- [x] Balagtas: `ProductController.java` `"StockSantan:"` → `"StockBalagtas:"` + comment. Confirmed remaining "Santan" hits are only the real street address `116 Santan St.` (app.js/index.html/docs) + DB backups + migration history — all left intact.
- [x] Nav item "Delivery Schedule" (`nav-delivery-schedule`, after Rejected Items) + `view-delivery-schedule` section (two empty cards: "Stock Moves", "Order Deliveries") + `loadDeliverySchedule()` stub + `navigateTo` hook (title + load call).
- [x] Cache-bust — `app.js?v=u11 → u12` (the token had advanced to u11 via the interim collections/cancel deploys; bumped to u12 when landing this session).
**Verify (clone):** new role assignable + user gets the tab; tab renders empty; edit-product activity log shows "StockBalagtas".
**Done when:** builds, tab visible to admins + Delivery Management, role saves.
**State after this session:** ✅ **DEPLOYED to production AND pushed to GitHub.** Landed via `feat/delivery-schedule-session0` (reconstructed cleanly from the original stash — no conflicts), merged to `main` (merge `aba1db0`), and rebuilt with `docker compose up -d --build`. Verified on live: backend healthy, Flyway still at **V91** (no migration ran, as expected), `db` container untouched; frontend serving `?v=u12` with the Delivery Schedule nav + `loadDeliverySchedule` stub. No DB migration; changes additive/low-risk. **2026-07-04: pushed to `origin/main`** together with the pizza-boxes-dispatched merge (`c90e5b6`) — `main` and `origin/main` are now in sync. Original stash dropped (fully captured in the deployed commit). Pending manual UI confirmation by user: role assignable, DELIVERY_MANAGEMENT user sees the empty tab, edit-product log reads "StockBalagtas". Note: frontend uses `ROLE_DEFAULT_PAGES` (not `PAGE_PERMISSIONS` as the reuse map said).
**Next session:** Session 1 (Stock Move backend, migration **V94**).

## Session 1 — Stock Move backend
**Goal:** full API for inter-warehouse moves; stock changes only on complete.
- [x] Migration **V94__stock_transfers.sql**: `stock_transfers` (status PENDING/APPROVED/COMPLETED/REJECTED/CANCELLED + `chk_stock_transfer_status`, scheduled_date, requested_by(+name), approved_by(+name), approved_at, completed_at, reject_reason, notes, change_log, created_at) + `stock_transfer_items` (transfer_id FK **ON DELETE CASCADE**, product_id, product_code, product_name, from_warehouse, to_warehouse, quantity>0 via `chk_stock_transfer_item_qty`). Indexes on status + transfer_id. New tables only; `movement_type='TRANSFER'` already allowed (no movement migration).
- [x] `StockTransfer.java` (EAGER items, `appendLog()` audit helper, `@JsonManagedReference`), `StockTransferItem.java` (`@JsonBackReference`), `StockTransferRepository.java` (`findAllByOrderByCreatedAtDesc`, `findByStatusOrderByCreatedAtDesc`), `StockTransferService.java`, `StockTransferController.java` (`/api/stock-transfers`).
- [x] Endpoints: `POST /` (create — any authed user), `GET /?status=`, `GET /{id}`, `POST /{id}/approve`, `/reschedule`, `/reject`, `/complete` (approver-gated), `/cancel` (approver **or** requester).
- [x] `complete`: `@Transactional`, guard status==APPROVED, delegates each line to new **`InventoryService.applyStockTransfer(...)`** — pessimistic-lock product, re-check source stock, reject SET products, deduct source + add dest + save + `logMovement('TRANSFER',…)`×2 → COMPLETED. Short-stock throws → whole complete rolls back.
- [x] Approver role set = **SUPER_ADMIN, ADMINISTRATOR, DELIVERY_MANAGEMENT** (in controller). Registered `/api/stock-transfers → delivery-schedule` in `PageAccessInterceptor` so the page gate applies.
**Verify (clone):** ✅ Restored latest prod backup (`..._0801_pre_fix421.sql`, at V91) into an isolated PG16; **V92 applied cleanly** (V91→V92). Constraint checks pass: bad status rejected (`chk_stock_transfer_status`), qty=0 rejected (`chk_stock_transfer_item_qty`). Simulated complete moved exact qty (wh1 1050→1040, wh3 0→10) with exactly two TRANSFER rows (−10/+10); FK `ON DELETE CASCADE` verified. Backend **compiles clean** (`mvn compile`, Java 21 container). Service guards (short-stock rollback, non-approver 403, reject/reschedule/cancel state guards) verified by compile + review; full REST-with-JWT E2E deferred to Session 2/5 (needs the UI + live auth). Clone torn down; no test data written to prod.
**Done when:** all endpoints pass clone checks. ✅ **Backend done — NOT yet deployed** (no frontend consumer until Session 2; deploy V92 with Session 2 so Phase A ships together, per the overview).
**State after this session:** Backend complete on branch **`main`** working tree (uncommitted). New files: migration `V94__stock_transfers.sql`; `StockTransfer/StockTransferItem/StockTransferRepository/StockTransferService/StockTransferController.java`. Edited: `InventoryService.java` (+`applyStockTransfer`), `PageAccessInterceptor.java` (+rule). Not committed, not deployed, not pushed — Flyway on live prod still at **V91**; V92 will first run when Session 2 deploys `docker compose up -d --build`. No frontend/cache-bust needed this session (no static changes). **Design notes for Session 2:** transfer JSON serializes items inline (managed/back reference); warehouse codes are wh1/wh2/wh3 with `wh3` shown as **"Balagtas"** (`StockTransferService.whLabel`); create body = `{ items:[{productId,fromWarehouse,toWarehouse,quantity}], scheduledDate, notes }`; SET products are rejected server-side (hide them in the move modal's type-ahead).
**Next session:** Session 2 (Stock Move frontend → Phase A ships, deploy V92 here).

## Session 2 — Stock Move frontend (→ Phase A ships)
**Goal:** usable request + approval UI.
- [x] Request modal (`modal-stock-move`): multi-line; product type-ahead (purpose-built `renderSmProductDropdown` reusing `appState.cachedProducts` + the same `.product-dropdown` CSS — `renderProductDropdown` itself is coupled to the order-form row IDs so it couldn't be called directly; SET products hidden); from/to selects (default source = warehouse with most stock, dest = Balagtas, editable per line); qty; per-line live source-stock hint with short-stock warning; submit `POST /api/stock-transfers`.
- [x] "Stock Moves" card: status filter dropdown + "New Stock Move" button; list requests as a table (# / details+lines / requested by+date / scheduled / status badge / actions); each line shows live per-WH source stock (red if < qty). Approver actions **Approve/Reschedule/Reject/Complete** and **Cancel** rendered per status/role; the four approver actions go through a unified `modal-stock-move-action` gated by the admin **security key** (`/api/auth/verify-security-key`, same pattern as delivery-edit) — reschedule adds a date field, reject a reason field; Cancel (requester **or** approver) is a plain confirm (no stock impact, no key).
- [x] Empty/error/loading states + `showToast` on every outcome.
- [x] Cache-bust `app.js?v=u12 → u13`.
**Verify (clone):** ⏳ **NOT yet run.** Frontend `node --check` passes (container); all new `onclick`/`$('id')` handlers cross-checked against defined `window.*` fns and DOM IDs; reused CSS classes confirmed present. Full clone E2E (request as normal user → approve → complete → quantities update) still pending — deferred pending user decision on deploy timing (see below), since the clone build also first-applies **V92**.
**Done when:** Phase A works end-to-end on the clone. — code complete; clone E2E + deploy pending.
**State after this session:** Frontend complete on `main` working tree (uncommitted), all in `index.html` + `js/app.js`. **Not committed, not deployed, not pushed.** V92 (Session 1) still not on live prod (Flyway at **V91**); it applies the moment Session 2 deploys via `docker compose up -d --build`. Because deploying is the outward-facing/prod step and Session 5 is the designated deploy-both session, **held for user go-ahead** on whether to (a) run the ephemeral-clone E2E + deploy Phase A now, or (b) land code only and defer deploy to Session 5. **Design notes:** create body = `{ items:[{productId,fromWarehouse,toWarehouse,quantity}], scheduledDate, notes }`; approver role check mirrored client-side in `isMoveApprover()` (SUPER_ADMIN/ADMINISTRATOR/DELIVERY_MANAGEMENT) but backend is authoritative; list needs `appState.cachedProducts` for live stock (auto-loaded in `loadStockMoves`).
**Next session:** Session 3 (Deferred Delivery backend, migration **V93**).

## Session 3 — Deferred Delivery backend
**Goal:** an order can be created deferred and fulfilled later, recording only on delivery day; safe cancel/reschedule.
- [x] Migration **V93__order_scheduled_delivery.sql**: adds `orders.scheduled_delivery_date DATE`, `orders.delivery_change_log TEXT`, `orders.delivered_at TIMESTAMPTZ` (all `ADD COLUMN IF NOT EXISTS`, nullable); **widened `chk_status`** (drop+recreate, superset) to include `'SCHEDULED_DELIVERY'`. Additive/reversible; no existing rows touched.
- [x] `OrderService.createOrder`: when `scheduledDeliveryDate` is present → `createScheduledDelivery(...)` saves as `SCHEDULED_DELIVERY` with an opening change-log line and **skips** stock/sale/commission/cash. Takes precedence over COD routing. Builder (`buildOrderFromRequest`) now maps `scheduledDeliveryDate` from the request; the create controller **skips its post-create commission call** for this status.
- [x] `fulfillScheduledDelivery(orderId,userId)` (modelled on `batchMarkAsCollected`): pessimistic-lock+items, guard `status==SCHEDULED_DELIVERY`, set `DELIVERED`+`delivered_at`+append log, `recordSale` **dated today**, commission, cash-if-CASH, then **deduct stock last** (short-stock throws → whole @Transactional rolls back). No daily-report patch needed — revenue is ledger-by-date (`getGrossSalesForDate`), so today's live report + close snapshot both capture it (unlike collect, which books to a past closed day).
- [x] `rescheduleDelivery(orderId,newDate,userId)`: guard `SCHEDULED_DELIVERY`, reject past dates, move date + append log; repeatable. `POST /{id}/reschedule-delivery` + `POST /{id}/fulfill-delivery` ("Mark Delivered" **and** "Deliver now" both land here). Added `GET /scheduled-deliveries` (list, newest-first) and a lightweight `POST /{id}/cancel-delivery` (reason only, **no security key / no void-cancel permission** — safe because nothing was recorded; rejects non-scheduled statuses).
- [x] `cancelOrder`: captures `wasScheduledDelivery` before the flip; for `SCHEDULED_DELIVERY` it sets CANCELLED + logs then **early-returns**, skipping VOID ledger + cash-reverse + stock-restore (nothing was ever recorded — avoids a phantom stock-in and negative ledger entry).
- [x] Added `'SCHEDULED_DELIVERY'` to every daily-report/dashboard/monthly-report exclusion: `DailyReportService` (all `NOT IN (...)` snapshot filters), `ReportsController` (SQL `NOT IN` + 2 Java stream filters), `DashboardController` (new `isBillable(Order)` helper replacing the `!CANCELLED` filters at all sales/analytics sites + `codPending` list). Day-close open-orders guard (`status IN ('ACTIVE','PENDING')`) already excludes it → scheduled orders never block a close. `OrderResponse` exposes `scheduledDeliveryDate`/`deliveredAt`/`deliveryChangeLog` for Session 4.
**Verify (clone):** ✅ Restored latest prod backup (`..._0801_pre_fix421.sql`, at **V91**) into an isolated PG16.14; **V92 then V93 applied cleanly** (V91→V92→V93, `ON_ERROR_STOP`). Confirmed the 3 new columns (date/text/timestamptz) + widened `chk_status`. Inserted a real `SCHEDULED_DELIVERY` order+item: **excluded** from the report-snapshot count and items-sold join (0), **does not** hit the day-close guard (0); garbage status rejected by `chk_status`; transition to `DELIVERED` allowed and the order **then counts** in the snapshot (1). Backend **compiles clean** (`mvn compile`, Java 21 container → BUILD SUCCESS). Full REST-with-JWT E2E of create/reschedule/fulfill/cancel deferred to **Session 5** (needs the Session-4 UI + live auth), mirroring Session 1's precedent. Clone torn down; **no test data written to prod**.
**Done when:** cancellation matrix + report-exclusion all pass. ✅ **Backend done — NOT yet deployed** (no frontend consumer until Session 4; V93 deploys alongside V92 when Phase A/B ships).
**State after this session:** Backend complete on **`main`** working tree (uncommitted). New file: migration `V93__order_scheduled_delivery.sql`. Edited: `Order.java` (+3 fields, `appendDeliveryLog`, `LocalDate` import), `dto/CreateOrderRequest.java` (+`scheduledDeliveryDate`), `dto/OrderResponse.java` (+3 fields), `OrderService.java` (builder map + `createScheduledDelivery`/`fulfillScheduledDelivery`/`rescheduleDelivery` + `cancelOrder` guard), `OrderController.java` (skip commission for scheduled + `GET /scheduled-deliveries`, `POST /{id}/fulfill-delivery`, `POST /{id}/reschedule-delivery`, `POST /{id}/cancel-delivery` + `convertToResponse`), `DailyReportService.java`, `ReportsController.java`, `DashboardController.java` (exclusions). Not committed, not deployed, not pushed — Flyway on live prod still at **V91**; V92+V93 first run when Phase A/B deploys via `docker compose up -d --build`. No frontend/cache-bust this session. **Design notes for Session 4:** create body adds `scheduledDeliveryDate:"YYYY-MM-DD"` (presence → deferred; omit for normal orders). Manage via `GET /api/orders/scheduled-deliveries`; per-order actions `POST /{id}/fulfill-delivery` (Mark Delivered **and** Deliver now), `POST /{id}/reschedule-delivery` `{scheduledDeliveryDate}`, `POST /{id}/cancel-delivery` `{reason}` — **none need the admin security key**. All under the `/api/orders`→"orders" page gate (DELIVERY_MANAGEMENT already has "orders"). Frontend derives **overdue** = `scheduledDeliveryDate < today`; **reschedule count** = `deliveryChangeLog` line count − 1. Also remove the dead `order_type=DELIVERY` `<option>` (index.html:592 + Add-Records) per locked decision 1. **Known limitation (acceptable, documented):** for a scheduled order created day X but fulfilled a *later* day Y, revenue lands correctly on day Y (ledger-by-date) but the operational **counts** (order/items) attribute to neither snapshot (snapshot counts are keyed by order-id prefix = creation date; day X excluded it while scheduled, day Y's prefix doesn't match). Financials are correct; only the operational tally is affected — flag for Session 5 E2E review.
**Next session:** Session 4 (Deferred Delivery frontend → Phase B ships).

## Session 4 — Deferred Delivery frontend (→ Phase B ships)
**Goal:** create + manage scheduled deliveries.
- [x] Order form: removed the dead `<option value="DELIVERY">` from **both** the live New Order type select (index.html) **and** the Add-Records type select. Added a "Schedule for later delivery" checkbox (`field-schedule-delivery`) + reveal-on-check date picker (`field-schedule-delivery-date`, `toggleScheduleDelivery()`, min=today) to the **live** order form only (backdated Add-Records is historical, so no scheduling there). `addOrder()` validates the date (required + not past) and sends `scheduledDeliveryDate` in the create body; `clearOrderForm()` resets it; success toast is scheduled-aware.
- [x] "Order Deliveries" card (`loadOrderDeliveries`/`renderOrderDeliveries`): loads `GET /api/orders/scheduled-deliveries`; table = Order (id+created) / Customer+items / Total / Scheduled (with **OVERDUE** badge when date < today + "rescheduled N×" from the change log) / Actions. Actions **Mark Delivered** (or **Deliver Now** when the scheduled date is still in the future — both call `POST /{id}/fulfill-delivery`; unified into one contextual button since the backend endpoint is identical), **Reschedule** (`POST /{id}/reschedule-delivery`), **Cancel** (`POST /{id}/cancel-delivery`). All three go through a keyless `modal-delivery-action` (`confirmDeliveryAction`) — **no admin security key** (a scheduled order recorded nothing), unlike the stock-move approver modal.
- [x] Oversell hint: `_odOversell()` compares each line's ordered qty against the product cache's live per-warehouse stock; a non-blocking "low stock for delivery" note shows in the list and an amber warning banner appears in the fulfil confirm (user can still proceed — backend deducts atomically + rolls back on a true shortfall). Product cache is refreshed after a fulfil so the next hint is accurate.
- [x] Cache-bust `app.js?v=u13 → u14`.
**Verify (clone):** ⏳ **Not yet run against a live backend.** Frontend `node --check` passes (container); all new `onclick`/`$('id')` handlers cross-checked against defined `window.*` fns + DOM IDs; confirmed the running static host serves the edits (cache-bust `u14`, `field-schedule-delivery`, `modal-delivery-action`, `loadOrderDeliveries`, and **zero** remaining `Delivery` options). Full clone E2E (create via form → 0 stock/sale + appears in tab → reschedule → fulfil records today + DELIVERED → cancel clean → deliver-now) needs the Session 3+4 backend running against a clone, so it **pairs with the Session 5 deploy** (Phase B backend V93 is not yet on live prod, same hold as Phase A/V92).
**Done when:** Phase B works end-to-end on the clone. — code complete; clone/live E2E + deploy pending.
**State after this session:** Frontend complete on `main` working tree (uncommitted), all in `index.html` + `js/app.js`. **Not committed, not deployed, not pushed.** V92 (Phase A) + V93 (Phase B) still not on live prod (Flyway at **V91**); both apply the moment this ships via `docker compose up -d --build`. **Design notes:** create body sends `scheduledDeliveryDate:"YYYY-MM-DD"` (presence → deferred). Manage via `GET /api/orders/scheduled-deliveries`; per-order `POST /{id}/fulfill-delivery` | `/reschedule-delivery {scheduledDeliveryDate}` | `/cancel-delivery {reason}` — none need a security key. Overdue = `scheduledDeliveryDate < today`; reschedule count = `deliveryChangeLog` "rescheduled" line count. Both features (Phase A stock moves + Phase B deliveries) now sit code-complete under the Delivery Schedule tab, held for the combined Session 5 deploy.
**Next session:** Session 5 (full E2E + deploy V93+V94 + commit/push).

## Session 5 — Integration, verification & deploy
**Goal:** ship both features to prod safely.
- [x] Full clone E2E: both features + cancellation matrix + role gating + regression — **48/48 passed** on an ephemeral clone (prod backup `..._0801_pre_fix421.sql` at V91 → Flyway applied V92→V93→V94; two test users injected into the clone for REST-with-JWT auth). Stock-move: create(non-approver)→approve(no stock change)→complete(exact ±10, 2 TRANSFER rows)→short-stock rollback→reject/cancel/reschedule + 403 role gating. Deferred: normal-order regression (deduct+SALE), scheduled records nothing + excluded from snapshot + pizza-box-dispatched, reschedule×2, fulfill (deduct+SALE dated today + then counts), cancel (nothing recorded), cancel-delivery guard. Clone torn down; no test data written to prod.
- [x] Deploy: `docker compose up -d --build` — pre-deploy prod backup `rrbm_db_backup_20260704_1546_predeploy_v94.sql` taken; Flyway applied **V93→V94** on top of live V92 (now at **v94**); backend healthy; frontend serving `?v=u15`.
- [x] Commit + push (frontend + backend) — landed via `feat/delivery-schedule-sessions1-4`, merged to `main` (no-ff), pushed to `origin/main`.
**Done when:** live prod healthy, both features verified, `main` in sync. ✅ **All done.**
**State after this session:** ✅ **SHIPPED to production + pushed to GitHub (2026-07-04).** Both features (Phase A inter-warehouse stock moves + Phase B deferred order delivery) live under the Delivery Schedule tab. Flyway at **V94**; app healthy; frontend at `?v=u15`. 48/48 clone E2E green before deploy. `main` and `origin/main` in sync.

## Session 6 — Edit & Confirm scheduled deliveries (V95)
**Goal:** let staff change a scheduled order's line items (product / qty / unit price / warehouse) before delivery, and require a **final-order confirmation** before it can be delivered.
**Locked decisions:** confirmation is a **persistent gate** (additive boolean `delivery_confirmed`); "Mark Delivered" is blocked (UI + backend) until confirmed; **editing items clears the confirmation** (re-confirm required). Editing is **keyless**; **anyone with the Delivery tab** can edit + confirm. Order stays `SCHEDULED_DELIVERY` throughout (no `chk_status` change; all existing report/inventory exclusions still apply). No stock/ledger reversal on edit — a scheduled order records nothing until fulfilled, so edit just rebuilds items + `calculateTotals()`; the SALE at fulfilment reads the recomputed `order.getTotal()`.
- [x] Migration **V95__order_delivery_confirmed.sql**: `orders.delivery_confirmed BOOLEAN NOT NULL DEFAULT FALSE` + `delivery_confirmed_at TIMESTAMPTZ` (both `ADD COLUMN IF NOT EXISTS`). Additive/reversible.
- [x] `Order.java` (+`deliveryConfirmed`/`deliveryConfirmedAt`), `OrderResponse.java` (+2 fields + `convertToResponse` mapping).
- [x] `OrderService`: `editScheduledDeliveryItems` (guard SCHEDULED_DELIVERY, validate like `buildOrderFromRequest`, rebuild items in place via `getItems().clear()`+`addItem`, set subtotal explicitly, apply discount/fee, `calculateTotals`, **reset confirmed=false**, log) + `confirmScheduledDelivery` (idempotent, set confirmed=true+timestamp, log) + **confirmed guard in `fulfillScheduledDelivery`** (403/400 "must be confirmed before delivery").
- [x] `OrderController`: `POST /{id}/edit-delivery-items` (CreateOrderRequest body — items/discount/deliveryFee) + `POST /{id}/confirm-delivery`; both keyless via `userIdFromHeader`, under the `/api/orders` page gate.
- [x] Frontend: `_odActionsHtml` — **Edit items** always; **Confirm final order** when unconfirmed (Mark Delivered hidden) / **Mark Delivered** once confirmed; ✓ Confirmed / awaiting-confirmation chip in the Scheduled cell. New `modal-delivery-edit` (multi-line item editor reusing the stock-move type-ahead pattern + `smWhOptions`, live total, discount/fee) + `modal-delivery-confirm` (read-only itemized review). Cache-bust `?v=u15 → u16`.
**Verify (clone):** ✅ Restored latest prod backup into isolated PG16; **V93→V94→V95 applied cleanly** (now at v95), both columns present. REST E2E (test users injected): create scheduled (unconfirmed) → **fulfill blocked** (400 "must be confirmed") → **edit items** (change qty, add/remove line; total recomputed 42→103.96→58.80; still SCHEDULED_DELIVERY; **no stock/sale**) → **confirm** (flag+timestamp set) → **edit-after-confirm resets** confirmed→false + clears timestamp → fulfill blocked again → re-confirm → **fulfill** records edited qty (stock −7) + edited total (SALE ₱58.80 dated today); guards (edit/confirm rejected on DELIVERED); keyless works as STANDARD_USER; validation (empty items / bad product → 400); cancel-delivery unaffected. Effectively **40/40** (one assertion used a stale hardcoded baseline; re-proved via before/after that edit deducts zero stock). Clone torn down; no test data written to prod.
**State after this session:** ✅ **SHIPPED to production + pushed (2026-07-06).** Flyway at **V95**; app healthy; frontend at `?v=u16`. **Behaviour note:** existing scheduled orders default to `delivery_confirmed=false`, so they must be confirmed once before delivery (the intended new gate, not a regression).

---

## Design reference (carry-over detail)
- **Move direction:** per-line `from_warehouse`/`to_warehouse` (flexible: pull from WH1 or WH2 → Balagtas, or reverse, in one request). UI pre-fills defaults; each line shows product's per-WH stock to prevent input errors.
- **Deferred-delivery lifecycle:** `SCHEDULED_DELIVERY` is inert (excluded from inventory + sales + reports). Terminal states only: `DELIVERED` (recorded on the real day) or `CANCELLED` (never recorded). Reschedule is repeatable and distinct from cancel. Missed date → stays scheduled + "overdue", never auto-acts.
- **Migrations are additive/reversible** (new tables, new nullable columns, widened CHECK). Back up prod before each deploy.
- **Balagtas rule:** all new labels/movement reasons say "Balagtas"; never "Santan". Do not alter `company_address` or applied migrations.
