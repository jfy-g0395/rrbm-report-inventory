# Maven / Java build output (do not upload)
rrbm-backend/target/

# IDE and editor folders
.idea/
.vscode/
*.iml

# OS junk files
Thumbs.db
.DS_Store
~$*

# Local secrets (create this file on your machine only — never commit passwords)
rrbm-backend/src/main/resources/application-local.properties
.env
.env.*
# ...but keep the committed template so a fresh clone has a variable reference
!.env.example

# Node
node_modules/
package-lock.json

# Captured build/test output (churns; not source)
rrbm-backend/test_output.txt
rrbm-backend/..backend-startup.log

# Database backups & data exports — kept locally as deploy safety nets, never source
rrbm_db_backup_*.sql
backup_err.log
_backfill/
_exports/
